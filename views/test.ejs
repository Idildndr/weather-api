<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
  <link rel="stylesheet" href="/static/styles/style.css">
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0" />
</head>
<body>

    <div id="container">



      <div class="py-5 text-center"  style="color: aliceblue; ">
            <img class="d-block mx-auto mb-2" src="<%= iconMappings[weatherIcon] %>" alt="" width="80" height="57">
            <h2>  <%= weather.name %>,<%= locationName.country %>    </h2>
            <p><%= locationName.name %></p>
             <% if (weather.dt !== undefined) { %>
              <% const utcTimestamp = weather.dt * 1000; // Convert seconds to milliseconds %>
              <% const timezoneOffset = weather.timezone * 1000; // Convert seconds to milliseconds %>
              <% const localTimestamp = utcTimestamp + timezoneOffset; %>
  
              <% const localDateTime = new Date(localTimestamp); %>
              <% const options = { day: 'numeric', month: 'long' }; %>
  
              <p><%= localDateTime.toLocaleDateString('tr', options) %>   <%= localDateTime.toLocaleTimeString('tr', { hour: '2-digit', minute: '2-digit' }) %></p>
            <% } %>
      </div>

          <div class="p-5 text-center bg-body-tertiary rounded-3 ml-5 mr-5" style="max-width: 1200px; width: 100%; overflow-x: auto;">
            <div class="card-container d-flex overflow-auto" style="max-width: 100vw; margin: 0 auto; "  >
                    <% for (let i = 0; i < 10; i++) { %>
                        <% const listItem = weatherDay.list[i]; %>
                        <% if (listItem) { %>
                            <div class="card shadow-sm me-3" style="flex: 0 0 auto; width: 15rem;">
                            <img class="weather-icon-image" src="<%= iconMappings[listItem.weather[0].icon] %>" alt="Weather Icon" style="fill: aliceblue;">
                                <div class="card-body"   style="">
                                    

                                  <div class="col card-item">
                                    <% if (listItem !== undefined) { %>
                                        <p> <%= listItem.main.temp %> °C</p>
                                    <% } %>
                                </div>

                                    <div class="col card-item">
                                      <% if (listItem.dt_txt !== undefined) { %>
                                          <% const dateTime = new Date(listItem.dt_txt); %>
                                          <% const options = { day: 'numeric', month: 'long' }; %>
                                          <p><%= dateTime.toLocaleDateString('tr', options) %>   <%= dateTime.toLocaleTimeString('tr', { hour: '2-digit', minute: '2-digit' }) %></p>
                                      <% } %>
                                  </div>
                   

                                  <div class="col card-item">
                                    <% if (listItem !== undefined) { %>
                                        <p> <%= listItem.weather[0].description %></p>
                                    <% } %>
                                </div>


                                  
                                  <!-- 
                                    <div class="d-flex justify-content-between align-items-center">
                                        <div class="btn-group">
                                            <button type="button" class="btn btn-sm btn-outline-secondary">View</button>
                                            <button type="button" class="btn btn-sm btn-outline-secondary">Edit</button>
                                        </div>
                                        <small class="text-body-secondary">9 mins</small>
                                    </div>
                                    -->
                                </div>
                            </div>
                        <% } %>
                    <% } %>
                </div>
        </div>
        
          
          <!--

        <p class="col-lg-8 mx-auto fs-5 text-muted">
          <div class="modal-body py-0">
            <div class="container text-center">
              <div class="row row-cols-1 row-cols-sm-2 row-cols-md-4">
                <div class="col">
                 
                  <% if (temperature !== undefined) { %>
                    <p>Temperature: <%= temperature %> °C</p>
                  <% } %>
                </div>
                <div class="col">
                  <% if (weatherMain !== undefined) { %>
                    <p>Weather: <%= weatherMain %></p>
                  <% } %>
                </div>
                <div class="col">
                  <% if (weatherDescription !== undefined) { %>
                    <p>Description: <%= weatherDescription %></p>
                  <% } %>
                </div>
                
            </div>        
          </div>
        -->
  
</div>
</div>
</body>
</html>